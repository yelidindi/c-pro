#include <stdio.h>
#include<string.h>
int main() {
    char str[1000];
    int i;
    gets(str);
    for(i=0;str[i]!='\0';i++)
    {
        if(str[i]=='a')
        printf("A");
        else
        printf("%c",str[i]);
    }
    return 0;
}
output:
aditya
AdityA
